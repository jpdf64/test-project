#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'master'
        }
    options {
        buildDiscarder(logRotator(numToKeepStr: '5', artifactNumToKeepStr: '5'))
        timestamps()
    }
    stages {
        stage("Step 1") {
            steps {
                sh 'ssh root@10.164.0.8 \'hostname\''
            }
        }
        stage("Step 2") {
            steps {
                sh 'ssh root@10.164.0.8 \'uptime\''
            }
        }
    }
}